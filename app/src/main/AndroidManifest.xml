<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Deliberately NO <uses-permission android:name="android.permission.INTERNET"/>.
      Without it Android blocks every socket this app opens, so "no network requests"
      is enforced by the OS rather than by promise. The app shows a badge that checks this at runtime.
    -->

    <application
        android:allowBackup="false"
        android:label="Offline Research"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
